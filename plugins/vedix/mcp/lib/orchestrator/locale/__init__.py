"""Per-locale citation/font/lint configuration (Block 6 — §6).

Each language module (en, ru, es, de, fr, zh, ja) exposes a ``CONFIG``
of type :class:`base.LocaleConfig`. The router (:mod:`router`) selects
the right module given an ISO 639-1 code.
"""
